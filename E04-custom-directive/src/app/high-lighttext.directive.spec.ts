import { HighLighttextDirective } from './high-lighttext.directive';

describe('HighLighttextDirective', () => {
  it('should create an instance', () => {
    const directive = new HighLighttextDirective();
    expect(directive).toBeTruthy();
  });
});
