import { OrdenacionPipe } from './ordenacion.pipe';

describe('OrdenacionPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenacionPipe();
    expect(pipe).toBeTruthy();
  });
});
